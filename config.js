/**
 * CMU — Konfigurasi API
 * 
 * GANTI nilai GAS_API_URL dengan URL deployment GAS Anda.
 * URL dapat ditemukan setelah deploy Web App di Google Apps Script.
 * Format: https://script.google.com/macros/s/[DEPLOYMENT_ID]/exec
 */
const CMU_CONFIG = {
    apiUrl: 'https://script.google.com/macros/s/GANTI_DENGAN_DEPLOYMENT_ID/exec'
};
